
// JavaScript has one number type — both integers and decimals are just number.


let int = 42;
console.log(int, typeof(int))
let float = 3.14;
console.log(float, typeof(float))
let negative = -100;
console.log(negative, typeof(negative))
let exponent = 5e3;     // 5000
console.log(exponent, typeof(exponent))

//Note : - int, float, negative, exponent is var name here, not data types.
// In JS there is only number for int and decimal